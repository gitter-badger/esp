/*
    start.me - MakeMe build file (esp-angular-mvc)
 */

Me.load({
    settings: {
        makeme: '0.8.0',
    },
    targets: {
        comp: {
            build: `run('esp -r -q compile')`,
        },
        run: {
            depends: ['comp'],
            run: 'appweb -v',
        },
        cleanCache: {
            type: 'clean',
            action: "rm('cache/*')",
        },
    },
})
