/*
    test.me - MakeMe file for ESP
 */

Me.load({
    targets: {
        test: {
            action: `run('testme --depth ' + me.settings.depth)`,
            platforms: [ 'local' ],
        },
        'clean-test': {
            type: 'clean',                                                                                 
            action: `run('testme --clean')`
        },  
    },
})
